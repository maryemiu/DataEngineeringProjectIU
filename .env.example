# =============================================================================
# Environment Variables — copy this to .env and fill in your values.
# The .env file is gitignored. NEVER commit real secrets to version control.
# =============================================================================

# PostgreSQL credentials
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme
POSTGRES_DB=recommendations

# JWT secret for the Recommendation API
# Generate a strong secret with: openssl rand -hex 32
JWT_SECRET=changeme

# Processing window — how many past days the Spark job reads (default: 7)
PROCESSING_WINDOW_DAYS=7
